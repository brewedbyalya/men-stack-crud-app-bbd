<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title><%= business.name %> | Business Directory</title> 
</head>
<body>
  <div class="container business-details">

    <header class="business-header">
      <h1><%= business.name %></h1>
      <% if (business.isVerified) { %>
        <span class="verified-badge">✅ Verified</span>
      <% } %>
    </header>

    <section class="business-info">
      <div class="info-item">
        <strong>Category:</strong>
        <span><%= business.category %></span>
      </div>

      <div class="info-item">
        <strong>Description:</strong>
        <p><%= business.description %></p>
      </div>

      <div class="info-item">
        <strong>Location:</strong>
        <span><%= business.location %></span>
      </div>

      <div class="info-item">
        <strong>Phone:</strong>
        <span><%= business.phoneNumber %></span>
      </div>

      <% if (business.website) { %>
      <div class="info-item">
        <strong>Website:</strong>
        <a href="<%= business.website %>" target="_blank" rel="noopener noreferrer">
          <%= business.website %>
        </a>
      </div>
      <% } %>
    </section>

    <div class="action-buttons">
      <a href="/businesses/<%= business._id %>/edit" class="button edit-button">
        Edit Business
      </a>
      
      <form action="/businesses/<%= business._id %>?_method=DELETE" method="POST" class="delete-form">
        <button type="submit" class="button delete-button">
          Delete Business
        </button>
      </form>
      
       <a class="businesses-index-link" href="/businesses">
      <div class="index-link-text">Back to All Businesses</div>
    </a>
    </div>
  </div>
</body>
</html>